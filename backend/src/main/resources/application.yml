spring:
  application:
    name: jobtrackr-ai-backend
  datasource:
    url: jdbc:postgresql://localhost:5432/jobtrackr_ai
    username: sharan
    password:
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
    open-in-view: false
  jackson:
    serialization:
      write-dates-as-timestamps: false

server:
  port: 8080

app:
  jwt:
    secret: ${JWT_SECRET:ZGV2ZWxvcG1lbnQtand0LXNlY3JldC1mb3Itam9idHJhY2tyLWFpLXRva2VuLTEyMzQ1Njc4OTA=}
    expiration-ms: ${JWT_EXPIRATION_MS:86400000}